= render 'shared/settings_links'

.row
  .col-md-8.col-md-offset-2
    - if @vk.errors.any?
      = render 'shared/error_message', object: @vk
    = form_for @vk, url: social_vk_path do |f|
      = render 'social/shared/form', f: f, object: @vk
    .sr-only= "group_id: #{@vk.group_id}"
