= render 'shared/settings_links'

.row
  .col-md-offset-4.col-md-4
    - if @in.errors.any?
      = render 'error_message', object: @in
    = form_for @in, url: social_in_path do |f|
      = render 'social/shared/form', f: f, object: @in
    .sr-only= "group_id: #{@in.group_id}"
