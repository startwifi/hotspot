= render 'shared/settings_links'

.row
  .col-md-offset-4.col-md-4
    - if @ok.errors.any?
      = render 'error_message', object: @ok
    = form_for @ok, url: social_ok_path do |f|
      = render 'social/shared/form', f: f, object: @ok
    .sr-only= "group_id: #{@ok.group_id}"
