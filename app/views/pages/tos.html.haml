= render 'shared/settings_links'

%h2= t('pages.tos')

.row
  .col-md-offset-3.col-md-6
    .jumbotron
      = markdown(@company.tos_text) if @company.tos_text.present?
    .text-center
      %p= link_to t('pages.edit'), tos_edit_pages_path, class: 'btn btn-primary'
